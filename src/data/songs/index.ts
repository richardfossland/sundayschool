import type { SeedSong } from '@/types/song'

// TEMPORARY STUB written by W6 because the real seed library was not yet present
// at typecheck time. The song-authoring worker owns this file and will replace
// it with the real curated `seedSongs`. Safe to overwrite.
export const seedSongs: SeedSong[] = []
