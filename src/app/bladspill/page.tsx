import { FagUnderConstruction } from '@/components/FagUnderConstruction'
import { SUBJECT_BY_ID } from '@/lib/subjects'

// Stub — v3 bølge 2 (W2A) erstatter innholdet.
export default function BladspillPage() {
  return <FagUnderConstruction subject={SUBJECT_BY_ID.bladspill} />
}
