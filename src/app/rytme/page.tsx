import { FagUnderConstruction } from '@/components/FagUnderConstruction'
import { SUBJECT_BY_ID } from '@/lib/subjects'

// Stub — v3 bølge 2 (W2B) erstatter innholdet.
export default function RytmePage() {
  return <FagUnderConstruction subject={SUBJECT_BY_ID.rytme} />
}
