import { FagUnderConstruction } from '@/components/FagUnderConstruction'
import { SUBJECT_BY_ID } from '@/lib/subjects'

// Stub — v3 bølge 2 (W2C) erstatter innholdet.
export default function LydteknikkPage() {
  return <FagUnderConstruction subject={SUBJECT_BY_ID.lydteknikk} />
}
